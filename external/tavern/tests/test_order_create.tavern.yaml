---
test_name: Create an order and get by identifier.

stages:
  - name: Create an order

    request:
      url: http://api-gateway:5566/orders
      method: POST
      json:
        products: [1, 2, 3,4]

    response:
      status_code: 200
      json:
        - !anystr
      save:
        json:
          uuid: "[0]"